# Ownable

Single-owner access with secure two-step ownership transfer.

## Functions

- `initialize(owner)` — set initial owner
- `transfer_ownership(new_owner)` — start transfer
- `accept_ownership()` — pending owner accepts
- `owner()` — query current owner
