# Testing with soroban-shield-tests

Use fuzz, property, and integration layers from the dedicated test repository.

```bash
git clone https://github.com/Soroban-shield/soroban-shield-tests
make test
```
